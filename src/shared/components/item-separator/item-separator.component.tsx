import * as React from 'react';
import { View } from 'react-native';

import { styles } from './item-separator.styles';

export const ItemSeparator = () => {
	return <View style={styles.separator} />;
};
